# test-whitestorm-webpack

> Test of using whitestorm.js with webpack

## Installation
```
npm install
```

## Commands
- `npm run build` to build the main bundle
- `npm run build:light` to bundle the light version (no physics)